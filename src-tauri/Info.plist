<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  与 Tauri 生成的 Info.plist 合并（Tauri 会自动读取与 tauri.conf.json 同目录的这个文件）。

  为什么必须有这个文件：**AppKit 注入到「窗口」菜单里的那些项，语言取自 App 包声明的本地化**。
  macOS 15 起系统会往任何带 `NSApp.windowsMenu` 角色的菜单里塞
  填充 / 居中 / 移动与调整大小 / 全屏幕平铺 / 从组中移除窗口 / 前置全部窗口 / 合并所有窗口 等项
  （我们通过 `WINDOW_SUBMENU_ID` 已拿到该角色）。这些字符串由系统提供、按**主包**的本地化解析：
  主包只声明 English 时它们就是英文 —— 于是整个中文界面里会冒出一排英文菜单项。

  实测（本机 macOS 27.0 / 系统语言 zh-Hans-US）：
  - 无 `CFBundleLocalizations` → Fill / Center / Enter Full Screen / Move & Resize / Full Screen Tile …
  - 声明 `zh-Hans`（或 `zh_CN`）→ 填充 / 居中 / 进入全屏幕 / 移动与调整大小 / 全屏幕平铺 …
  唯一变量、结论明确；XMind 也是靠包内 `zh_CN.lproj` 拿到中文的那一套。

  同时保留 `en`：英文系统下仍按英文显示（`CFBundleLocalizations` 是"支持哪些"，不是"强制哪一个"）。
-->
<plist version="1.0">
<dict>
  <key>CFBundleLocalizations</key>
  <array>
    <string>zh-Hans</string>
    <string>zh_CN</string>
    <string>en</string>
  </array>
  <!--
    语音备注要录音，macOS 上**没有这个键就根本拿不到麦克风**：
    `getUserMedia` 会以 NotAllowedError 拒绝，而且**不会弹授权对话框**
    （系统认为这个 App 没打算用麦克风）。WebKit 那一层的媒体捕获授权
    wry 已经实现并直接返回 Grant（见 wry 的 wry_web_view_ui_delegate.rs），
    所以真正的关卡只剩这一条声明 + 用户自己的系统授权。

    文案会原样出现在系统授权对话框里，所以要写清"录什么、存在哪"。
  -->
  <key>NSMicrophoneUsageDescription</key>
  <string>用于在主题上录制语音备注。录音只保存在你自己打开的文档里，不会上传。</string>
</dict>
</plist>
