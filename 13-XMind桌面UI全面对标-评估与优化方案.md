# 13 - XMind 桌面 UI 全面对标：评估与优化方案

> 目标：**先做到桌面 UI 与布局完全一致，再根据桌面 UI 反推并补齐功能与使用体验**
> 评估对象：MindGrid v0.3.3（Tauri 2 + React 19 + Vite）
> 对标基准：XMind 桌面端（2024/25 订阅版）
> 评估日期：2026-09-04
> 状态：**待确认后实施**

---

## 0. 结论摘要

上一轮（12 号文档）的 P0/P1 声称已完成"贴合式扁平布局"，经代码核查**大部分属实，但有 4 处关键落差**，其中 1 处是**方向性错误**：

| 结论 | 说明 |
|---|---|
| ✅ 骨架已达成 | 44px 工具栏通栏、`gap: 0`、去圆角、画布无描边投影、accent 令牌已为 `#2d7ff9` |
| ❌ **方向错误** | 分页标签被移到**画布列顶部**；而 XMind 实际在**底部状态条左侧**。需反向改回 |
| ❌ 未达成 | 节点仍在用渐变填充；40 处硬编码 accent 导致**暗色主题实际未打通**；12 处毛玻璃残留 |
| ⚠️ 架构不同 | 左栏为堆叠表单（非 3 Tab 导航）；右栏 4 Tab（XMind 为 3 子页）；无原生菜单栏 |

本轮分三批推进，**全部纳入实施**（已确认）：

- **批次 A** — 布局骨架与信息架构（分页栏归位、状态栏重建、左右面板改 Tab、原生菜单）
- **批次 B** — 视觉一致性（令牌治理、去渐变、清毛玻璃、暗色打通）
- **批次 C** — 功能与体验补齐（统计信息、缩放控件、查找替换、笔记面板、标记标签筛选、提案简报）

---

## 1. XMind 桌面端基准（调研结论）

### 1.1 布局骨架

```
┌────────────────────────────────────────────────────────────┐
│ 菜单栏   文件 · 编辑 · 视图 · 插入 · 格式 · 工具 · 帮助      │  Windows/Linux 窗口内
├────────────────────────────────────────────────────────────┤  macOS 为系统菜单栏
│ 工具栏   左：标题+同步  中：主题/分支/关连/总结/外框/插入      │  44px
│          右：甘特图 / 提案简报 / 评论 / 图标 / 格式(开关)     │
├──────────┬──────────────────────────────────┬──────────────┤
│ 左侧      │                                  │  右侧格式面板  │
│ 导航面板  │          中央画布                 │  样式|演说|画布│
│ 主题|笔记 │      背景 #F7F7F8                 │  （可开关）    │
│ |标记标签 │      无描边 / 无圆角 / 无投影      │               │
├──────────┴──────────────────────────────────┴──────────────┤
│ 状态条   Map 1 | Map 2 | +            统计 · 缩放100% · 大纲 │  28px 通栏
└────────────────────────────────────────────────────────────┘
```

### 1.2 各区域规范

| 区域 | 规范 | 来源置信度 |
|---|---|---|
| 菜单栏 | 7 个顶层菜单；macOS 走系统菜单栏，Windows/Linux 窗口内 | 高（多源一致） |
| 工具栏 | 单条 ~44px；左=标题/同步状态，中=元素工具，右=视图与模式切换 | 高 |
| 左侧面板 | **导航面板**，3 Tab：主题（大纲树+查找替换）/ 笔记（缩略图）/ 标记&标签（可作筛选） | 中（见 1.3） |
| 右侧面板 | **格式面板**，由工具栏「格式」按钮开关，3 子页：**样式 / 演说 / 画布** | 高（官方培训材料与教程一致） |
| 画布 | 背景 `#F7F7F8`，无限画布，空格+拖动平移，滚轮缩放 | 高 |
| 底部状态条 | **左：分页标签（Map 1/2…）+ 新建**；**右：统计信息（主题数/字数/字符数）+ 缩放比例 + 大纲切换** | 高（XMind 官方用户指南） |
| 主色 | 品牌蓝 `#2D7FF9`，纯色无渐变 | 高 |
| 分隔 | 1px 发丝线，无间隙、无圆角、无投影 | 高 |

**右侧格式面板内容**（官方培训材料）：
- **样式**：形状、文本（字体/大小/颜色/对齐）、结构、分支、编号
- **演说**：Pitch 主题风格、长宽比例
- **画布**：结构、配色方案、背景颜色、全局字体、分支线粗细、彩虹分支、自定义风格、高级布局（自动平衡/紧凑型/同级主题对齐/分支自由布局/灵活自由主题/主题层叠）

### 1.3 版本差异与置信度说明（重要）

左侧面板的描述在各资料间**不一致**，这是最大的不确定点：

| 来源 | 左侧栏描述 |
|---|---|
| XMind 官方功能/菜单说明 | 导航面板 3 Tab：主题 / 笔记 / 标记和标签 |
| XMind 官方用户指南（xmind.cn） | 右下角「大纲」入口，左下角为画布栏 |
| 第三方教程 A | 模板库 / 主题库 / 图标库 |
| XMind 2021 Mac 介绍 | 样式 / 格式 / 图标 |
| XMind ZEN 2020 介绍 | 结构库 / 风格编辑器 / 图标库 / 概要与外框 |

→ **处理方式**：本方案采用官方功能说明的「导航面板 3 Tab」作为基准。
→ **强烈建议**：提供一张目标版本 XMind 的实际截图，据此微调（见第 7 节）。

---

## 2. MindGrid 现状核查（代码事实）

> 核查方式：直接读代码与 CSS，**不采信 12 号文档的结论**。所有数值均为实际读取。

### 2.1 已达成项（P0/P1 属实部分）

| 项 | 代码位置 | 实测值 |
|---|---|---|
| 贴合式布局骨架 | `global.css:35-44` | `grid-template-rows: 44px minmax(0,1fr)`、`gap: 0`、`height: 100vh` |
| 三栏无间隙 | `global.css:47-56` | `grid-template-columns: 280px minmax(0,1fr) 280px`、`gap: 0` |
| 工具栏 | `global.css:985-995` | `height: 44px`、`border-radius: 0`、`border-bottom: 1px` |
| 图标按钮 | `global.css:1077-1093` | `height: 28px`、`min-width: 28px`、`border-radius: 6px` |
| 画布 | `global.css:2143-2147` | 背景 `#f7f7f8`、无描边、无圆角、无投影 |
| 状态栏 | `global.css:2942-2956` | `min-height: 28px`、通栏、`border-top: 1px` |
| accent 令牌 | `tokens.css:37` / `:97` | 亮色 `#2d7ff9`、暗色 `#6f9dff` |
| 检查器折叠分区 | `inspector.tsx:27` | `PanelSection` 组件，8 个分区 |
| 无重复渲染 | — | `WorkspaceScreen`/`Toolbar`/`SheetTabBar` 均只挂载一次，无重复 |

### 2.2 未达成项（含对文档的证伪）

| # | 文档声称 | 代码事实 | 判定 |
|---|---|---|---|
| 1 | "Sheet 标签移至工具栏正下方 36px" | 渲染在 `.canvas-column` 内顶部（`workspace-screen.tsx:213`），**被 280px 侧栏挤压、不通栏**；CSS `min-height: 30px`（`global.css:140`） | **方向错误 + 未通栏** |
| 2 | "主色全量切换，清除硬编码旧蓝" | `global.css` 仍有 **40 处** `rgba(45, 127, 249, *)`；源码 8 处文件残留 **26 处**旧蓝常量 | **未达成** |
| 3 | "去渐变，主按钮改纯色" | `global.css:2667-2680` 节点仍用 `linear-gradient`（中心节点 `135deg` 双色渐变） | **未达成** |
| 4 | "暗色经 token 自动适配" | 40 处硬编码 accent 不随令牌切换，暗色下仍为浅蓝 | **证伪** |
| 5 | "去毛玻璃" | 12 处 `backdrop-filter`；`global.css:975-981` 的重置块清了 `border/radius/box-shadow` 却**漏掉 `backdrop-filter: blur(20px)`** | **未清干净** |
| 6 | — | `src-tauri` 无 `MenuBuilder`/`SubmenuBuilder`，**无原生菜单栏** | 缺失 |

**硬编码 accent 分布**（`global.css` 40 处，抽样）：`:1073` `:1217` `:1540` `:1541` `:1554` `:1756` `:1834` `:1839` `:1879` `:1883` `:1924` `:1928` …

**毛玻璃分布**（12 处）：`:116`(标签胶囊) `:143`(标签栏) `:314`/`:491`(大纲/甘特头) `:843`(快捷键遮罩) `:981`(面板/画布/状态栏重置块) `:1466`(遮罩) `:1779`(右键菜单) `:2233`(缩放条) `:2247`(小地图) …

**渐变分布**（8 处，其中 4 处为网格纹理、1 处为分隔线，需处理 3 处）：
- `:2669` 中心节点 `linear-gradient(135deg, rgba(45,127,249,.96), rgba(111,176,255,.92))`
- `:2675` `:2679` 左/右节点白色微渐变

---

## 3. 差距清单

图例：**P0** = 布局/方向性错误，必须改 · **P1** = 视觉一致性 · **P2** = 功能补齐

| # | 差距 | 优先级 | 批次 |
|---|---|---|---|
| G1 | 分页标签位置错误（应底部通栏，现顶部画布列内） | **P0** | A1 |
| G2 | 状态栏缺统计信息 / 缩放比例 / 大纲入口 | **P0** | A2 |
| G3 | 左栏为 3 区堆叠表单，非 3 Tab 导航面板；缺笔记、标记标签 Tab | **P0** | A3 |
| G4 | 右栏 4 Tab（topic/canvas/relationships/grouping），应为样式/演说/画布 3 子页 | **P0** | A4 |
| G5 | 无原生菜单栏 | **P0** | A5 |
| G6 | 40 处硬编码 accent，暗色未打通 | **P1** | B1 |
| G7 | 节点渐变填充 3 处 | **P1** | B2 |
| G8 | 12 处毛玻璃残留（含重置块漏网） | **P1** | B3 |
| G9 | 旧蓝常量 26 处（需分类治理，见 5.2） | **P1** | B4 |
| G10 | 分隔符等中性色硬编码（`toolbar__divider` 用 `rgba(0,0,0,0.1)`，`:1163-1169`） | P1 | B5 |
| G11 | 无统计信息（主题数/字数/字符数） | P2 | C1 |
| G12 | 缩放控件浮于画布右下，未入状态栏 | P2 | C2 |
| G13 | 无查找/替换 | P2 | C3 |
| G14 | 无笔记面板 | P2 | C4 |
| G15 | 无标记与标签筛选 | P2 | C5 |
| G16 | 无提案简报（Pitch）模式 | P2 | C6 |

---

## 4. 优化方案

### 批次 A — 布局骨架与信息架构

#### A1. 分页标签归位底部状态条（G1）

**目标**：`SheetTabBar` 从画布列顶部移出，并入底部状态条左侧，通栏。

**改动**：
1. `src/features/workspace/workspace-screen.tsx:213` — 从 `.canvas-column` 内移除 `<SheetTabBar />`；删除 `:164` 注释中"渲染在画布列顶部"的过期描述
2. `src/features/status/status-bar.tsx`（42 行）— 重构为左/右两组：左侧渲染 `SheetTabBar`，右侧渲染统计/缩放/大纲
3. `src/styles/global.css` — `.sheet-tab-bar` 区块（`:134-292`）迁入状态栏区（`:2941-3043`），与 `.status-bar` 合并为一条 28px 通栏；`min-height` 30px → 随状态栏统一

**验收**：多画布时标签横跨整个窗口宽度，不被 280px 侧栏切断；ZEN / 大纲 / 甘特模式下隐藏。

#### A2. 状态栏重建（G2）

**目标**：右侧依次放置 统计信息 · 缩放比例 · 大纲切换。

**改动**：`status-bar.tsx` 重写（现 42 行 → 约 90 行）。
- 左：`<SheetTabBar>`（来自 A1）
- 右：统计信息（C1 提供数据）+ 缩放控件（C2 提供）+ 大纲切换按钮

**验收**：与 XMind 顺序一致；窄窗口下右侧组不换行。

#### A3. 左侧栏改为 3 Tab 导航面板（G3）

**目标**：`主题（大纲树 + 查找替换）` / `笔记` / `标记 & 标签（可筛选）`。

**改动**：`src/features/workspace/sidebar.tsx`（1284 行）
- 现有 3 个堆叠分区（`:655` 文档导航 / `:670` 画布管理 / `:917` 当前画布大纲）改为 Tab 容器
- 新增 Tab 头组件（复用 `inspector.tsx:27` 的 `PanelSection` 与 `:1994-2043` 的 Tab 条样式，保持视觉一致）
- 主题 Tab 内嵌现有大纲树（`:1178`）+ 查找替换（C3）
- 笔记 Tab、标记标签 Tab 由 C4 / C5 填充

**迁移决策**：原「画布管理」的画布增删改名、拖主题跨画布等能力不能丢 → 收进「主题」Tab 底部折叠区，或改为画布 Tab。

**验收**：3 Tab 可切换；大纲树交互（选中、展开折叠、拖拽）无回归。

#### A4. 右侧格式面板改为 3 子页（G4）

**目标**：`样式` / `演说` / `画布`。

**改动**：`src/features/workspace/inspector.tsx`（1607 行）
- `InspectorTab` 类型（`:216`）由 `'topic' | 'canvas' | 'relationships' | 'grouping'` 改为 `'style' | 'pitch' | 'canvas'`
- 现有 8 个 `PanelSection`（`:582` 主题属性 / `:598` 富内容 / `:981` 主题图片 / `:1047` 跨画布移动 / `:1186` 画布信息 / `:1201` 画布设置 / `:1217` 文档主题 / `:1258` 分支样式 / `:1376` 关系线 / `:1468` 边界与概要）重新归位：
  - **样式** ← 主题属性、富内容编辑、主题图片、分支样式
  - **演说** ← 新增（C6）
  - **画布** ← 画布信息、画布设置、文档主题、关系线、边界与概要、跨画布移动

**风险**：`workspace-screen.tsx:63-75` 与 `:157` 有 `inspectorTabRequest` 机制（工具栏「插入」菜单会请求切到 topic tab），需同步改枚举值。

#### A5. 原生菜单栏（G5）

**改动**：`src-tauri/src/` 新增菜单模块（约 300-400 行）+ `lib.rs` 挂载。
- 用 `tauri::menu::{MenuBuilder, SubmenuBuilder}` 构建 7 个顶层菜单
- macOS 自动走系统菜单栏；Windows/Linux 窗口内显示（需确认 `tauri.conf.json` 窗口配置）
- 菜单项复用现有快捷键（参考 `src/features/shortcuts/`）

**验收**：三平台菜单可见；菜单项触发与工具栏/快捷键同一命令路径。

---

### 批次 B — 视觉一致性与令牌治理

#### B1. 令牌治理：40 处硬编码 accent（G6）

**改动**：`src/styles/global.css` 40 处 `rgba(45, 127, 249, α)` → 新增令牌。

建议在 `src/styles/tokens.css` 增加 alpha 阶梯令牌，避免为 40 处各造一个变量：
```css
--color-accent-08: rgba(45, 127, 249, 0.08);
--color-accent-10: rgba(45, 127, 249, 0.10);
--color-accent-12: rgba(45, 127, 249, 0.12);
--color-accent-20: rgba(45, 127, 249, 0.20);
--color-accent-28: rgba(45, 127, 249, 0.28);
--color-accent-40: rgba(45, 127, 249, 0.40);
--color-accent-56: rgba(45, 127, 249, 0.56);
--color-accent-72: rgba(45, 127, 249, 0.72);
```
暗色区（`:75-111`）覆盖为 `#6f9dff` 的同 alpha 值。

**验收**：`grep -c "rgba(45, 127, 249" src/styles/global.css` → 0；切换暗色主题后无浅蓝残留。

#### B2. 去渐变（G7）

**改动**：`global.css:2669`（中心节点）、`:2675`（左节点）、`:2679`（右节点）→ 纯色。
- 中心节点：`#2d7ff9`（走 `var(--color-accent)`）
- 左/右节点：`var(--color-surface)` 纯白

**注意**：节点填充色若与画布运行时常量（`runtime/style-constants.ts`）耦合，需同步（见 5.1）。

#### B3. 清毛玻璃（G8）

**改动**：`global.css` 12 处 `backdrop-filter` 删除；背景改为纯色令牌。
- **重点**：`:975-981` 重置块补上 `backdrop-filter: none`（P1 的漏网之鱼）
- 浮层类（右键菜单 `:1779`、缩放条 `:2233`、小地图 `:2247`）改纯白 + 发丝边 + 轻投影，去 blur

#### B4. 旧蓝常量分类治理（G9）— **不可盲改**

26 处旧蓝**性质不同，必须分类处理**：

| 文件 | 处数 | 性质 | 处置 |
|---|---|---|---|
| `runtime/style-constants.ts` | 8 | 画布 UI 外壳（选中/激活/历史聚焦描边） | ✅ 改为 accent |
| `runtime/rich-content-constants.ts` | 3 | 标签胶囊底/文字色 | ✅ 改为 accent 系 |
| `canvas-host.tsx` | 4 | 视口外壳（框选、指示线等） | ✅ 改为 accent |
| `minimap.tsx` | 1 | 视口外壳 | ✅ 改为 accent |
| `inspector.tsx` | 1 | 面板 UI | ✅ 改为 accent |
| `global.css` | 1 | 样式表残留 | ✅ 改令牌 |
| `canvas/markers.tsx` | 5 | **标记语义色**（优先级/进度/旗帜，属用户内容） | ⚠️ 保留（对齐 XMind 标记色板，非 accent） |
| `lib/document/themes/built-in-themes.ts` | 3 | **文档主题色板**（用户可见、已持久化在既有文档） | ⚠️ 保留（改动会改变既有文档外观） |

**理由**：Markers 与 Themes 是用户内容配色，不是 UI 外壳。XMind 的标记色板同样不是品牌蓝。盲改会破坏既有文档视觉与导出一致性。

#### B5. 中性色走令牌（G10）

`global.css:1163-1169` `.toolbar__divider` 的 `rgba(0,0,0,0.1)` → `--color-border-default`（暗色下当前几乎不可见）。

---

### 批次 C — 功能与体验补齐

| # | 功能 | 实现要点 | 依赖 |
|---|---|---|---|
| C1 | 统计信息（主题数/字数/字符数） | 从 `session.summary` 扩展：`topicCount` 已有；新增 `wordCount` / `charCount`（Rust 端遍历主题文本累加） | A2 |
| C2 | 缩放控件入状态栏 | 现有浮点缩放条（`global.css:2223`）的能力下沉到状态栏；画布内保留小地图 | A2 |
| C3 | 查找 / 替换 | 新增 `src/features/search/`；遍历当前画布主题文本，支持下一个/上一个/替换/全部替换 | A3 |
| C4 | 笔记面板 | 列出全文档带备注的主题，点击定位 | A3 |
| C5 | 标记与标签筛选 | 列出全文档 marker/label 及计数，点击按条件高亮或过滤 | A3 |
| C6 | 提案简报（Pitch）模式 | 与现有演示模式（`presentation/`）区分：分支式逐幕展示 + 主题风格/长宽比设置 | A4 |

---

## 5. 关键风险与约束

### 5.1 三端渲染一致性（最高风险）

本项目画布有 **DOM / Canvas(PNG) / SVG(PDF)** 三套渲染，几何与配色常量必须同步。

- `runtime/style-constants.ts`、`runtime/rich-content-constants.ts` 是共享常量源（B4 会改动）
- 改动会同时影响**导出的 PNG/SVG/PDF** 配色
- 现有测试会打断：`canvas-renderer.test.ts`(8) `svg-renderer.test.ts`(6) `style-resolver.test.ts`(4) `svg-inner-canvas.test.ts`(1) 共 **19 处断言**需同步更新
- **必须跑** `dev/export-parity.*` 对照工具验证（Canvas vs SVG 差值应维持 ~0.8% 的抗锯齿水平，不出现结构性偏移）

### 5.2 主题色板不可动

`built-in-themes.ts` 的配色已持久化在用户既有 `.mgd` 文档中。改动会**静默改变已保存文档的外观**，且不可逆。不在本轮范围。

### 5.3 分批与回滚

- 每批次独立提交、独立跑全量门禁，任一批次失败可单独回滚
- 建议每批打一个 `v0.4.x`，避免大批次一次性上线

### 5.4 测试基线

当前基线：**518 测试 / 45 文件**（2026-09-04 实测）。
A3/A4 改动 info 架构会打断以 role/label 查询的断言，需同步更新测试而非放宽断言。

---

## 6. 验收门禁

每批次完成后，以下**四项全绿**方可进入下一批：

```bash
./node_modules/.bin/tsc -b --force      # 类型检查（勿用 tsc --noEmit，恒退 0）
./node_modules/.bin/vitest run          # 前端测试（基线 518）
./node_modules/.bin/oxlint src          # 0 error（16 warning 为既有基线）
cargo test --manifest-path src-tauri/Cargo.toml
```

批次 B 额外：
- `grep -c "rgba(45, 127, 249" src/styles/global.css` → **0**
- `grep -c "backdrop-filter" src/styles/global.css` → **0**
- 暗色主题下走查一遍主界面

批次 A/B 额外：
- 跑 `dev/export-parity.*` 对照，确认无结构性偏移

---

## 7. 待确认事项

| # | 事项 | 影响 |
|---|---|---|
| 1 | **提供目标版本 XMind 界面截图** | 各版本左侧栏差异大（见 1.3），有截图可避免二次返工 |
| 2 | A3 中「画布管理」（画布增删改名、跨画布拖拽）迁移到哪个 Tab？ | 建议收进「主题」Tab 底部折叠区，待确认 |
| 3 | C6 提案简报与现有演示模式（`presentation/`）是并存还是合并？ | 建议并存：演示=全屏渐进揭示，Pitch=分幕展示 |
| 4 | A5 原生菜单是否需要完整 7 个顶层菜单，还是先做「文件/编辑/视图」3 个？ | 影响工作量 |

---

## 8. 工作量估算（仅供排期参考）

| 批次 | 涉及文件 | 预估改动量 | 说明 |
|---|---|---|---|
| A | `workspace-screen.tsx` `status-bar.tsx` `sidebar.tsx` `inspector.tsx` `global.css` + Rust 菜单 | ~1200-1600 行 | 信息架构重构，风险最高 |
| B | `global.css` `tokens.css` `style-constants.ts` `rich-content-constants.ts` `canvas-host.tsx` `minimap.tsx` `inspector.tsx` | ~120 处替换 | 机械性为主，但需同步测试 |
| C | 新增 `features/search/` + 扩展 Rust summary + 面板逻辑 | ~900-1400 行 | 新功能，可独立排期 |
