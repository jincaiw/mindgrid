# XMind 桌面 UI 对标 · 第二轮（截图逐项评估与实施方案）

> 日期：2026-09-05
> 依据：用户提供的 19 张 XMind 桌面版真实截图（主界面、右栏三个子页、骨架选择浮层、配色方案浮层、工具栏插入弹层、8 个原生菜单全展开）。
> 上一轮方案（13 号文档）基于公开资料推断，左栏形态是"最大不确定点"；本轮截图落地后该不确定点已消除。
> 结论先行：**大骨架（44px 工具栏 / 左 3 Tab / 右 样式·演说·画布 3 子页 / 底部状态条 / 原生菜单）与 XMind 一致，本轮不动骨架**；差距集中在工具栏形态、右栏各子页内部项、菜单项级对齐，以及一批 XMind 有而 MindGrid 无的能力。

---

## 1. 逐区域差距清单

### 1.1 顶部工具栏（差距：大，形态性）

| XMind（截图） | MindGrid 现状 | 差距 |
|---|---|---|
| 左上：文档标题 + 「已编辑」状态，无任何文件按钮 | 左段塞了文件名 + 保存状态 + 新建/打开/保存/另存为/撤销/重做/导入▾/导出▾/模板 共 12 个控件 | 文件操作 XMind 全部收进菜单，工具栏不放 |
| 中段 7 项，**图标在上、小字在下**竖排：主题 / 子主题 / 联系 / 概要 / 外框 / 标记 / 插入▾ | 中段文字按钮：新建子主题 / 新建同级 / 删除主题 / 插入▾ / 结构▾ / 主题▾ | 按钮集合不同、视觉形态完全不同 |
| 右段 3 项：ZEN / 演说 / 格式（同样图标+小字） | 右段 10 个按钮：搜索/侧栏/检查器/大纲/甘特/专注/演示/检查更新/快捷键/主题外观 | 需收敛 |
| — | 删除主题按钮 | XMind 工具栏无删除（右键菜单承担） |

**方案（批次 D）**：
1. 工具栏改为 XMind 三段式：
   - **左段**：文档标题 + 保存状态（保留现有），移除文件操作/撤销重做/导入导出/模板按钮（功能保留在菜单与快捷键，模板入口移到「文件 → 新建」）
   - **中段**（图标+小字竖排）：`主题`（=新建同级/在之后插入）、`子主题`、`联系`、`概要`、`外框`、`标记`、`插入▾`（插入弹层见 1.7）
   - **右段**：`ZEN`、`演说`、`格式`（=切换右栏）；另有意保留两个小图标：明暗主题切换、快捷键帮助（收为纯图标，不占文字位）
2. 被移除入口的去向：撤销/重做（菜单+⌘Z 已有）、搜索（⌘F+菜单已有）、大纲/甘特（查看菜单+状态条）、结构/主题下拉（画布子页骨架卡片与配色方案承接，见 1.2）、检查更新（帮助菜单已有）
3. 新增竖排按钮样式：`.toolbar__item{flex-direction:column;gap:2-4px}` + 11px 小字，图标沿用 `icons.tsx` 体系补齐缺的（联系/概要/外框/标记 专用图标）

### 1.2 右栏「画布」子页（差距：大，本轮核心）

对照截图自上而下：骨架卡片 → 配色方案 → 配色预览网格 → 背景颜色 → 全局字体 → 分支线粗细 → 彩虹分支 → 导图样式(3 checkbox) → 高级布局(3 checkbox) → 中日韩字体。

| # | XMind 项 | MindGrid 现状 | 方案（批次 E） |
|---|---|---|---|
| 1 | **骨架卡片**：当前结构缩略图+名称+▾，点开浮层分组展示 9 组骨架（思维导图/逻辑图/括号图/组织结构图/树形图/时间轴/鱼骨图/树型表格/矩阵图），每组多张缩略图 | 工具栏「结构▾」文字下拉 9 项；左栏藏着一个图表类型下拉；无浮层、无缩略图 | 新增 `structure-picker` 浮层：分组标题 + 缩略图卡片（纯 SVG 迷你图静态绘制，不跑布局引擎）。点击写 `sheet.chartType`。**树型表格 MindGrid 无此布局**：卡片置灰标「开发中」（是否实施见待确认 #1） |
| 2 | **配色方案**：下拉（当前"彩虹"）+「+」；浮层「缤纷/经典」两 Tab，缤纷 12 套命名配色（彩虹/活力/舞动/代码/和风/岛屿/玫瑰/薄荷/绿茶/宇宙/精致/纯真） | 5 套文档主题（经典蓝/暗夜/暖阳/青松/极简），无浮层无预览 | 新增 12 套主题数据（**只新增不改现有 5 套色值**——红线）；浮层带「缤纷/经典」Tab，经典=现有 5 套；每套画迷你导图 SVG 预览；「+」按钮占位置灰（自定义配色见待确认 #1） |
| 3 | **配色预览网格**：3×2 主题缩略卡片，点选即换 | Inspector 画布页已有 5 张色卡（较简陋） | 并入 #2 的浮层与面板：面板上直接放 6 张缩略卡（当前方案 + 推荐 5 张） |
| 4 | **背景颜色**：色块拾色 | ❌ 无，背景随主题写死 | `document.settings['canvas.background']`（空=跟随主题）；`style-resolver` 优先读它；DOM/Canvas/SVG 三端同源自动一致；UI 为颜色块+清除 |
| 5 | **全局字体**：下拉「默认」 | ❌ 字体栈硬编码常量 | `settings['canvas.fontFamily']`：默认/系统默认/宋体/黑体/楷体/等宽；`style-resolver` 的 FONT_FAMILY 改为可覆盖，三端同源 |
| 6 | **分支线粗细**：下拉「默认」 | 仅样式子页有 0.5–3× 滑杆 | 画布页加下拉：默认/细/中/粗（映射 0.75/1/1.5/2 乘数），与滑杆同一数据源 |
| 7 | **彩虹分支**：checkbox + 色轮▾ | 有等价能力（分支色板=每分支一色）但无该命名与开关 | 画布页加 checkbox（勾=启用 colorPalette，取消=单色）+ 色板下拉（现有 5 套预设迁移过来） |
| 8 | **导图样式**：自动平衡布局 / 紧凑型布局 / 同级主题对齐 | ❌ 无（`LayoutConfig.direction` 类型存在但引擎未读） | `LayoutConfig` 扩展 `balance/compact/alignSiblings`，mindmap/logic 布局引擎实现；布局引擎是三端共享的（buildScene），DOM/Canvas/SVG 自动同步 |
| 9 | **高级布局**：分支自由布局 / 灵活自由主题 / 主题层叠 | 自由主题有（双击画布创建）；其余无 | `灵活自由主题` = 允许/禁止自由主题开关（绑定双击创建行为）；`分支自由布局`、`主题层叠` 置灰占位（大工程，待确认 #1） |
| 10 | **中日韩字体**：下拉「默认」+说明 | ❌ 无独立项 | `settings['canvas.cjkFont']` 开关（默认/开启 CJK 优化），合并进字体栈的 CJK 段 |

### 1.3 右栏「样式」子页（差距：大，重排+三个新能力）

XMind 顺序：**选中主题预览块 → 形状 → 文本 → 结构 → 分支 → 编号**。

| # | XMind 项 | MindGrid 现状 | 方案（批次 F） |
|---|---|---|---|
| 1 | 顶部选中主题**预览块**（黄底圆角块实时显示样式+▾层级选择） | ❌ 只有文字「当前主题」 | 顶部加 DOM 迷你预览块（复用节点内联样式：填充/文字/圆角实时反映）；层级批量下拉暂缓（待确认 #1） |
| 2 | **形状**：形状类型▾ + 填充▾+色块 + 边框▾+色块 + 线型▾ + 宽度(数字 PX + 适合) | 有：形状 4 种（圆角/直角/胶囊/下划线）、节点色覆盖、边框粗细滑杆 | 重排为 XMind 布局：形状▾ / 填充色 / 边框色 / 宽度数字输入；「适合」按钮置灰占位 |
| 3 | **文本**：字体▾+字号 / 字重▾+颜色 / B I S̶ Tʀ / 对齐三连 | 有：字号滑杆、字重 4 档；无字体族、无 B/I/S 快捷开关、无对齐 | 补：字体族下拉（同全局）、B/I/删除线三toggle（写入 styleOverrides）、对齐三连（新 `textAlign` override）；Tʀ 暂缓 |
| 4 | **结构**：结构▾ + 方向▾（向右） | ❌ 样式页无；方向无实现 | 样式页加结构下拉（9 种，与画布页骨架同源）+ 方向下拉（向右/向左/自动平衡）——**实现布局方向翻转**（mindmap/logic/tree 的 dx 取反），补 `LayoutConfig.direction` 的引擎读取 |
| 5 | **分支**：线型▾ + 线条样式▾+终点▾ + 粗细▾+颜色 | 有：曲线/直线/折线、粗细滑杆、5 套色板 | 保留现有，补**终点样式**（无/箭头/圆点）——三端渲染：DOM(CSS)/Canvas(path)/SVG(marker) |
| 6 | **编号**：样式▾（无/1.2.3./…）+「应用于兄弟主题」 | ❌ 全无 | 新功能：`sheet.settings['topic.numbering']`：none/arabic(1. 2.)/arabic-paren(1) 2))/letter-upper/lower/cjk(一、二、)/circled(①②)；**在 scene-builder 生成前缀**→三端+导出自动一致；「应用于兄弟主题」=把当前编号设置应用到同层所有分支（sheet 级设置天然如此，按钮置灰或做 topic 级覆盖，见待确认 #2） |
| 7 | XMind 样式页**没有**：备注/链接/标签/标记/任务/图片 | MindGrid 样式页有且完善 | **有意保留**（移除是功能倒退）；顺序下移到编号之后作为折叠区，见 §3 有意差异 |

### 1.4 右栏「演说」子页（差距：中）

| XMind | MindGrid 现状 | 方案（批次 G） |
|---|---|---|
| 演说模式预览（当前幻灯片缩略图，「当前幻灯片不显示。」空态） | 无预览 | 预览块：用 Canvas Runtime 把当前幕渲染成小图（空态文案对齐 XMind） |
| 「更换风格」按钮 | 简报主题风格是**静态文字** | 控件化：风格下拉（跟随文档/深/浅） |
| 长宽比（自动） | 静态文字 | 控件化：自动/16:9/4:3/1:1（`pitch-controller` 已支持，只差 UI 接线） |
| 主题幻灯片（自动） | 无 | 下拉：自动/每分支一页（映射现有 DFS 逐节点 vs Pitch 分幕两种模式的选择，二者已并存） |

### 1.5 原生菜单（差距：中，项级对齐）

XMind 顶层顺序：**Xmind(应用) / 文件 / 编辑 / 插入 / 工具 / 查看 / 窗口 / 帮助**；MindGrid 现为：文件/编辑/视图/插入/格式/工具/帮助。

**方案（批次 D）**：
1. 顶层重排为 `文件 / 编辑 / 插入 / 工具 / 查看 / 窗口 / 帮助`；**删除「格式」菜单**（6 个结构项由画布子页骨架卡片承接）
2. **文件**（对标）：快速新建⌘N(=现有新建) / 新建…(=模板选择) / 新建标签页⌘T(=新建画布) / 打开⌘O / 保存⌘S / 另存为⇧⌘S / 导入▸(Markdown/OPML/Word) / 导出▸(Markdown/OPML/PNG/PDF/SVG) / 关闭窗口(系统预置)；「最近打开」「分享」「打印」「设置密码」置灰占位（无底层，见待确认 #1）
3. **编辑**（对标）：撤销/重做/剪切/拷贝/粘贴/复制⌘D/**删除单个主题**/拷贝样式⌥⌘C/粘贴样式⌥⌘V/**重设样式**⌥⌘0(=清除样式覆盖，会话已有能力只差菜单)/前往中心主题⌘R(自视图移入)/全选/展开子主题/展开所有子分支(新增 canvasCommand)/查找与替换⌘F
4. **插入**（对标，只列已实现）：子主题/主题(之后)/主题(之前)/父主题/联系/概要/外框/笔记/标签/任务/链接/标记/本地图片/新画布⌥⌘N/从主题新建画布；「标注/贴纸/插画/附件/语音备注/方程/自由主题」不列（无底层，见待确认 #1）
5. **工具**：检查更新 / 快捷键 / 明暗外观；「Map Shot 截图/合并文件/自定义风格」置灰占位（待确认 #1）
6. **查看**（对标）：思维导图/大纲(radio 勾选) / 放大⌘+ 缩小⌘- 实际大小⌘0 适应画布(接入 zoomRequest nonce 通道) / ZEN 模式 / 演说模式 / 导航面板 / 格式面板 / 工具栏(新增显隐) / 显示标签页栏⇧⌘T(新增)；「仅显示该分支」待确认 #1
7. **窗口**：Tauri `PredefinedMenuItem`（最小化/缩放/关闭等系统项）
8. 两侧 id 契约照旧由 `menu-actions.test.ts` 锁定；**继续不注册系统快捷键**，新增快捷键全部落前端 registry（⌘T、⌘+/-/0、⇧⌘T、⌥⌘0、⌘D 补登记；ZEN 保持 ⌘. 并加 ⌥⌘F 双绑定对齐 XMind）

### 1.6 底部状态条（差距：小）

XMind 右段：`主题: 1/15 | 100%⇅ | 大纲`；左段无标签（标签页栏独立可开关 ⇧⌘T）。

**方案（批次 D）**：
- 右段统计改为 **`主题: 选中序号/总数`**（如 `主题: 3/15`；多选时 `已选 3/15`），字数统计并入悬停 title（信息不丢）
- 缩放按钮、大纲按钮保留；「最近动作」并入 title
- 左段标签页栏保留，新增 `⇧⌘T` 显隐开关（状态持久化 sessionStorage，同侧栏做法）

### 1.7 工具栏「插入」弹层（差距：小）

XMind 点击「插入▾」弹出菜单：笔记/标签/任务/链接▸/附件/语音备注/贴纸/插画/本地图片/方程。
**方案（批次 D）**：现有插入下拉重排为同顺序，只列已实现项（笔记/标签/任务/链接/标记/本地图片），其余不列；样式对齐 XMind 弹层（白底圆角+发丝线，已有 context-menu 样式可复用）。

---

## 2. 红线（实施时不可破坏）

1. `built-in-themes.ts` 现有 5 套主题与 `markers.tsx` 21 个标记的**色值一字不改**——用户内容配色，改了会静默改变既有 `.mgd` 外观；新增配色全部走新增 id
2. DOM CSS 是几何基线；新增布局选项（balance/compact/align/direction）、编号、终点样式必须走共享 scene-builder/布局引擎，DOM/Canvas/SVG 三端自动一致，改完跑 `dev/export-parity` 对照
3. 菜单 Rust/TS 两侧 id 契约由 `menu-actions.test.ts` 锁定，两侧同批改
4. oxlint 基线 16 warning / 0 error；CI 五步门禁（含 CLI 构建、release check）推前本地预跑
5. 原生菜单不注册系统快捷键（避免双触发），快捷键全部进前端 registry

## 3. 有意差异（不照抄 XMind 的部分）

| 差异 | 理由 |
|---|---|
| 样式页保留 备注/链接/标签/标记/任务/图片 分组 | XMind 把这些放插入弹层，但 MindGrid 已有完整编辑 UI，移除是功能倒退；下移折叠即可 |
| 工具栏右段保留 明暗切换、快捷键 两个小图标 | 桌面常用，XMind 在首选项里，就地保留更顺手 |
| ZEN 快捷键保留 ⌘. 并加 ⌘⌥F | ⌘. 已发布给用户，双绑定兼容 |
| 状态条保留字数统计（进 title） | 对标显示 `主题: 1/15`，但字数信息有价值，收纳不删除 |
| 左栏 3 Tab 结构 | 截图中 XMind 未展开左栏，上一轮已按官方指南对齐，本轮不动 |

## 4. 分批实施

| 批次 | 内容 | 主要文件 | 规模 |
|---|---|---|---|
| **D 骨架对齐** | 工具栏三段式重做（图标+小字）、原生菜单重排+补项+「窗口」菜单、删除「格式」菜单、插入弹层重排、状态条 `主题: n/N`、标签页栏开关、快捷键补登记 | `toolbar.tsx`、`menu.rs`+`menu-actions/dispatch.ts`+测试、`status-bar.tsx`、`icons.tsx`、`global.css`、`shortcuts/*` | 中 |
| **E 画布子页** | 骨架浮层（缩略图）、配色方案系统（+12 套主题+浮层+预览卡）、背景颜色、全局字体/中日韩、分支线粗细、彩虹分支、导图样式 3 选项（布局引擎）、自由主题开关 | `inspector.tsx`、新 `structure-picker.tsx`/`palette-picker.tsx`、`built-in-themes.ts`(追加)、`style-resolver.ts`、布局引擎、`types.ts`、Rust settings 通道 | 大 |
| **F 样式子页** | 预览块、形状/文本/结构/分支/编号 重排、方向翻转、终点样式（三端）、编号（三端） | `inspector.tsx`、`style-resolver.ts`、`scene-builder.ts`、`canvas-renderer.ts`、`svg-renderer.ts`、布局引擎、`types.ts` | 大 |
| **G 演说子页+收尾** | 演说预览缩略图、长宽比/风格/幻灯片控件化、 parity 全量对照、视觉走查 | `pitch-view.tsx`、`inspector.tsx` | 小 |

建议顺序 D → E → F → G，每批独立过五绿门禁、独立提交。D/E 落地后界面即与截图肉眼级一致；F 是功能深化；G 收尾。

## 5. 待确认问题（实施前需要拍板）

1. **P2 大工程做不做**（本轮截图出现但 MindGrid 无底层）：树型表格布局、标注(callout)、贴纸/插画/语音备注/附件/方程、仅显示该分支、Map Shot 选区截图、合并文件、自定义风格编辑器、背景色之外的最近打开/打印/分享/设置密码。→ 建议本轮全部置灰占位不做，占位入口让 UI 与 XMind 形态一致
2. **编号的应用范围**：XMind 有「应用于兄弟主题」按钮（topic 级覆盖）。建议第一版只做 sheet 级（整张画布统一编号规则），按钮不做；够用后再加 topic 级
3. **工具栏文件按钮去留**：方案 D 会把 新建/打开/保存/另存为/导入/导出 从工具栏移除（XMind 如此，功能都在菜单+快捷键）。若希望保守，可保留「保存」一个按钮
4. **「结构」「主题」下拉从工具栏移除**后完全由右栏画布子页承接（XMind 如此）。若使用频率高，也可在插入▾里保留二级入口
